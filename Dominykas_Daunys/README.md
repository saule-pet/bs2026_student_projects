# Automatization of invoice generation and email sending

The code is supposed to:
- scan your calendar;
- calculate how much each person must pay you (based on provided rates);
- generate invoices (.docx);
- save them as pdf;
- send emails with invoices